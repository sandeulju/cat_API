import React from "react";
import { ProfileBackground, ProfileWrapbox } from "./Styles";

const CatProfile = () => {
  return (
    <ProfileWrapbox>
      <ProfileBackground />;
      <ProfileBackground />;
      <ProfileBackground />;
    </ProfileWrapbox>
  );
};

export default CatProfile;
